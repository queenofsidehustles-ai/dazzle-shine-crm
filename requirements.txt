# Pinned so every instance runs what was tested. Bumps here are deliberate:
# a version moves when it fixes a published vulnerability, not because it is
# newer. See SECURITY.md for what is outstanding and why.
flask==3.1.3
flask-sqlalchemy==3.1.1
alembic==1.16.5
psycopg2-binary==2.9.9
requests==2.32.5
stripe==10.4.0
python-dotenv==1.2.1
gunicorn==23.0.0
twilio==9.3.7
cloudinary==1.40.0
cryptography>=42.0.0
tzdata
