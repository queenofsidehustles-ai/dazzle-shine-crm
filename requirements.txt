# Pinned so every instance runs what was tested. Bumps here are deliberate:
# a version moves when it fixes a published vulnerability, not because it is
# newer. See SECURITY.md for what is outstanding and why.
flask==3.1.3
flask-sqlalchemy==3.1.1
alembic==1.16.5
psycopg2-binary==2.9.9
requests==2.33.0
stripe==10.4.0
python-dotenv==1.2.2
gunicorn==23.0.0
twilio==9.3.7
cloudinary==1.40.0
cryptography>=42.0.0
tzdata

# Pinned explicitly although nothing here imports them: they arrive through
# twilio, requests and flask, and the versions those resolve to on their own
# still carry published vulnerabilities. A transitive dependency is no less
# reachable for being unmentioned.
aiohttp==3.14.3
urllib3==2.7.0
click==8.3.3
